Date Functions in DAX

TODAY
Today Date = TODAY()

YEAR
Year Column = YEAR(Sales[OrderDate])

MONTH
Month Column = MONTH(Sales[OrderDate])

DAY
Day Column = DAY(Sales[OrderDate])

FORMAT
Month Name = FORMAT(Sales[OrderDate], "MMMM")